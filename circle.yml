node:
  verson: 7.9.0
dependencies:
  pre:
    - pip install awsebcli
    - npm install
test:
  override:
    - echo "done"
deployment:
  production:
    branch: production
    commands:
      - npm run pack
      - eb deploy LearningObjectSubmission-env --profile default
